version: '3.8'

services:

  backend:
    build: ./backend
    container_name: lab27-backend
    ports:
      - "5002:5002"
    environment:
      MONGO_URI: mongodb+srv://golipavansaikrishna2608_db_user:LbMQXDcPkhjAL5Ie@cluster0.bvyloaz.mongodb.net/?appName=Cluster0

  frontend:
    build: ./frontend
    container_name: lab27-frontend
    ports:
      - "3002:3002"
    environment:
      PORT: 3002
    depends_on:
      - backend